🚨 DO THIS RIGHT NOW (5 MINUTES) 🚨

═══════════════════════════════════════════════════════════════

STEP 1: ROTATE CLOUDINARY CREDENTIALS
--------------------------------------

1. Go to: https://cloudinary.com/console
2. Login to your account
3. Click "Settings" (gear icon)
4. Click "Security" tab
5. Scroll to "API Keys"
6. Click "Regenerate API Secret"
7. Copy the NEW credentials:
   - Cloud Name: _______________
   - API Key: _______________
   - API Secret: _______________

═══════════════════════════════════════════════════════════════

STEP 2: UPDATE LOCAL .ENV FILE
-------------------------------

Open: backend/.env

Replace these lines with YOUR NEW credentials:

CLOUDINARY_CLOUD_NAME=your_new_cloud_name_here
CLOUDINARY_API_KEY=your_new_api_key_here
CLOUDINARY_API_SECRET=your_new_api_secret_here

Save the file.

═══════════════════════════════════════════════════════════════

STEP 3: UPDATE RENDER
----------------------

1. Go to: https://dashboard.render.com/
2. Select your backend service
3. Click "Environment" in sidebar
4. Update these 3 variables with NEW values:
   - CLOUDINARY_CLOUD_NAME
   - CLOUDINARY_API_KEY
   - CLOUDINARY_API_SECRET
5. Click "Save Changes"
6. Render will auto-redeploy

═══════════════════════════════════════════════════════════════

STEP 4: TEST YOUR APPLICATION
------------------------------

1. Wait for Render to finish deploying (~3 minutes)
2. Go to your website
3. Login as admin
4. Try uploading an image to Gallery
5. Check if it works

If it works, the new credentials are active! ✅

═══════════════════════════════════════════════════════════════

STEP 5: CLEAN GIT HISTORY (IMPORTANT!)
---------------------------------------

The OLD credentials are still in Git history!

Option A - Simple (but loses history):
```
git checkout --orphan new-master
git add .
git commit -m "Clean repository - removed exposed credentials"
git branch -D master
git branch -m master
git push -f origin master
```

Option B - Use the cleanup script:
```
# On Windows:
powershell -ExecutionPolicy Bypass -File clean-git-history.ps1

# On Mac/Linux:
bash clean-git-history.sh
```

Option C - Create new repository:
1. Create new repo on GitHub
2. Push clean code to new repo
3. Delete old repo
4. Update Render/Vercel to use new repo

═══════════════════════════════════════════════════════════════

✅ DONE CHECKLIST:

□ Rotated Cloudinary credentials
□ Updated backend/.env
□ Updated Render environment variables
□ Tested application (image upload works)
□ Cleaned Git history
□ Pushed clean repository

═══════════════════════════════════════════════════════════════

⏰ TIME REQUIRED: 5-10 minutes

Do this NOW before someone exploits your exposed credentials!

═══════════════════════════════════════════════════════════════
